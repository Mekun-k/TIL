2024/07/18の積み上げ

コミットログが重複している場合、それを整理するために`git rebase`を使用することができます。以下の手順で、重複しているコミットを削除しましょう。

1. **`git rebase -i` コマンドを使用してインタラクティブなリベースを開始します**：

   重複しているコミットの数に応じて、適切な数を指定してください。ここでは、最新の7つのコミットをインタラクティブリベースする例を示します。

   ```bash
   git rebase -i HEAD~7
   ```

2. **エディタが開いたら、重複しているコミット行を削除します**：

   開いたエディタで、コミット行が以下のように表示されます。

   ```
   pick d9afcb4 add: 再プッシュ
   pick 67d1de5 Edit: ルーティングを編集
   pick a58fbdc add: 非同期通信によって、お気に入りの更新・削除を可能にした
   pick d9afcb4 add: 再プッシュ
   pick 67d1de5 Edit: ルーティングを編集
   pick a58fbdc add: 非同期通信によって、お気に入りの更新・削除を可能にした
   ```

   重複している行を削除して、一意のコミットのみを残します。

   ```
   pick d9afcb4 add: 再プッシュ
   pick 67d1de5 Edit: ルーティングを編集
   pick a58fbdc add: 非同期通信によって、お気に入りの更新・削除を可能にした
   ```

3. **エディタを保存して閉じます**：

   これにより、重複するコミット行が削除され、リベースが実行されます。

4. **リベースが成功したら、リモートリポジトリを強制プッシュします**：

   変更をリモートリポジトリに反映させるために、以下のコマンドを使用して強制プッシュします。

   ```bash
   git push --force
   ```

これで、重複していたコミットが削除され、コミットログが整理されるはずです。コミットの重複を整理する際には注意が必要ですので、必要に応じて作業前にバックアップを取ることをお勧めします。
