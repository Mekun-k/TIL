2024/07/11の積み上げ

まずは、ajaxの処理についてどのような技術を使用するのか調べる。

```php
自分の考え
・まずは、ブックマークのフォームのオプションを非同期通信で送信できるように変更する

っていうよりajaxの流れを理解しているのか？
まず、HTTPリクエストが送信されるのか？
MVCのどこまでリクエストが届くのかわかっていない

まずは、最初の20分間で全体像を把握する。
やりたいことを言語化する

わかっていることとしては、ブックマーク保存の際にviewを返さずにjsonを返しているということ。
あとは、JSの拡張子？でフォームから情報を送信する必要がある。

まず、ブックマークボタンがクリックされた時にイベントが発火するように設定する
それからJavascript XML　をrouteに渡す。
routeからコントローラーに処理を渡す。
そこから処理を実行して最後にビューを返さずにjsonを渡す

じゃあ、まずは、ボタンをクリックした時にどのようなイベントを発火させるか考える。
普通に更新or削除の処理を実行してもらうために、bookmarkコントローラーのアクションを実行させたいな
そのためには、いろいろとデータを渡す必要がありそうやけどな

てか、Jqueryを書くには、breadファイル内に書くのか、jsファイルを作成してそこに記述するのかどっちがええんやろうか

```

疑問点

- なぜ、JSON形式でレスポンスを返す必要があるのだろうか？
- なぜ、ボタンにクラスを追加したのか
- なぜ、片方のボタンのみにidを設定したのか
- 以下のコードの説明
    
    ```php
    // 部分ビューをレンダリングしてHTMLとして取得
    $html = View::make('boards.partials.bookmark_button', ['board' => $board])->render();
    
    // JSON形式でHTMLを返す
    return response()->json(['html' => $html]);
    
    ```
    

まず、bookmarkそのものを更新すれば動的に変更されるとみた。

そのため、
